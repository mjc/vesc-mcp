# PLAN-6 final decision: evidence-complete local research stack

PLAN-6 is complete. Sequential stage gates rejected the original assumption
that every local model should be enabled. The recommended production path is:

1. mandatory fielded lexical retrieval with exact identifiers protected;
2. Granite Embedding 97M R2 INT8 CPU query embeddings;
3. no reranker by default;
4. authoritative deterministic Rust investigation contracts;
5. deterministic, verifier-backed graph and tagged-history relationships;
6. no planner model and no critic model by default;
7. a frontier answerer reachable only after all mandatory repository × stage ×
   era facets and relationships are covered and subsequently cited.

The Ryzen 5 8600G and Apple M1 use native ONNX CPU lookup. The RX 5700 XT is
used only for the exact measured Jina FP16 bulk-ingestion profile. M1 Core ML
and every evaluated Vulkan planner/critic remain non-default because they
failed their respective execution or quality gates.

The locked historical loader case completes with six facets and five
relationships. Removing decisive ChibiOS evidence returns an actionable
insufficiency report and never calls the answerer. Hard rules, fast planner,
and planner+critic evaluation rows all record `FrontierShortcutRate = 0`.

Run `python3 scripts/verify-plan-6.py` to verify all machine-readable decisions,
model/license pins, hardware scope, locked path result, and component-specific
verifiers. Any model, quantization, runtime, provider, hardware, prompt, or
budget upgrade requires a new bounded benchmark.
